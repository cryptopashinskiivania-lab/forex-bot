# 🚀 Деплой Data Quality System на сервер

## 📋 Что было добавлено в этом обновлении

### ✅ Критические исправления:
1. **Таймзона ForexFactory** - исправлена с Europe/Kyiv на America/New_York (события были на 7-10 часов неправильно)
2. **AI Forecast фильтрация** - теперь анализирует только будущие события, а не прошедшие
3. **Умная дедупликация** - объединена логика aggregateCoreEvents с приоритетами
4. **AI Results валидация** - требует и прогноз И факт для полного анализа
5. **Обнаружение конфликтов** - между ForexFactory и Myfxbook

### ✨ Новые возможности:
- **DataQualityService** - модуль валидации, фильтрации и обнаружения конфликтов
- **Ежедневные отчеты** - автоматические отчеты о качестве данных в Telegram
- **Алерты админам** - уведомления о критических проблемах (с throttling)
- **12 unit-тестов** - полное покрытие тестами
- **Тестовые скрипты** - для проверки календаря, AI фильтров, конфликтов

---

## 🔧 Процедура деплоя на сервер

### Шаг 1: Подключитесь к серверу

```bash
ssh root@YOUR_SERVER_IP
# или ваш метод подключения
```

### Шаг 2: Перейдите в директорию бота

```bash
cd /root/forex-bot
# или путь, где установлен бот
```

### Шаг 3: Обновите код с GitHub

```bash
git pull origin main
```

**Ожидаемый вывод:**
```
From https://github.com/cryptopashinskiivania-lab/forex-bot
 * branch            main       -> FETCH_HEAD
Updating ae44045..9bcdf9d
Fast-forward
 30 files changed, 4729 insertions(+), 103 deletions(-)
 create mode 100644 src/services/DataQualityService.ts
 create mode 100644 src/types/DataQuality.ts
 ...
```

### Шаг 4: Установите зависимости

```bash
npm install
```

**ВАЖНО:** Убедитесь, что установлены dev-зависимости для TypeScript:
```bash
npm install --save-dev @types/better-sqlite3 @types/node
```

### Шаг 5: Пересоберите проект

```bash
npm run build
```

**Проверьте, что нет ошибок компиляции!**

Ожидаемый вывод (успех):
```
> forex-news-bot@1.0.0 build
> tsc

# Без ошибок - значит успех
```

### Шаг 6: Проверьте базу данных

База данных автоматически расширится новыми полями при первом запуске.
Но можно проверить структуру:

```bash
sqlite3 forex-news.db ".schema data_issues"
```

### Шаг 7: Перезапустите бота

#### Если используется PM2:
```bash
pm2 restart forex-bot
pm2 logs forex-bot --lines 100
```

#### Если используется systemd:
```bash
sudo systemctl restart forex-news-bot
journalctl -u forex-news-bot -f -n 100
```

#### Если запущен вручную:
```bash
pkill -f "node.*bot"
nohup npm run dev > bot.log 2>&1 &
tail -f bot.log
```

---

## ✅ Проверка работы

### 1. Проверьте логи при старте

В логах должны появиться сообщения о DataQualityService:

```bash
pm2 logs forex-bot --lines 200 | grep -E "(DataQuality|filterForDelivery|checkCrossSource)"
```

### 2. Проверьте работу AI Forecast

Отправьте в Telegram боту команду `/daily` и нажмите кнопку **"🧠 AI Forecast"**

**Ожидаемое поведение:**
- Анализирует только будущие события
- В логах: `[Bot] AI Forecast: X events skipped due to quality issues`
- События без времени или в прошлом пропускаются

### 3. Проверьте работу AI Results

После выхода важного события с фактическими данными, нажмите **"📊 AI Results"**

**Ожидаемое поведение:**
- Анализирует только события с прогнозом И фактом
- В логах: `[Bot] AI Results: X events skipped due to quality issues`

### 4. Проверьте дедупликацию

```bash
pm2 logs forex-bot --lines 200 | grep -E "(aggregateCoreEvents|deduplication)"
```

**Ожидаемый вывод:**
```
[Aggregation] Starting event aggregation
[Aggregation] ForexFactory: 5 events
[Aggregation] Myfxbook: 87 events
[Aggregation] Deduplication: 8 duplicates found
[Aggregation] Selected event with more data: Federal Funds Rate
```

### 5. Проверьте обнаружение конфликтов

```bash
pm2 logs forex-bot --lines 300 | grep "cross-source conflicts"
```

Если оба источника активны (FF + Myfxbook), должны быть сообщения о найденных конфликтах.

### 6. Проверьте data_issues в базе данных

```bash
sqlite3 forex-news.db "SELECT type, source, COUNT(*) as count FROM data_issues WHERE created_at > datetime('now', '-1 day') GROUP BY type, source;"
```

**Пример вывода:**
```
TIME_INCONSISTENCY|Myfxbook|156
NO_TIME|ForexFactory|3
MISSING_REQUIRED_FIELD|ForexFactory|1
```

---

## 🧪 Дополнительное тестирование

### Запустить ежедневный отчет о качестве

```bash
npm run quality:report
```

Отчет автоматически отправится в ADMIN_CHAT_ID в Telegram.

**Формат отчета:**
```
📊 Daily Data Quality Report

Period: Last 24 hours
Total Issues: 160

Issues by Type:
  • TIME_INCONSISTENCY: 156 (97.5%)
  • NO_TIME: 3 (1.9%)
  • MISSING_REQUIRED_FIELD: 1 (0.6%)

Issues by Source:
  • Myfxbook: 156 (97.5%)
  • ForexFactory: 4 (2.5%)
```

### Запустить тесты (опционально)

```bash
# Все unit-тесты
npm test

# Конкретные тесты
npm run test:calendar
npm run test:ai-results
npm run test:cross-source
```

---

## 📊 Мониторинг после деплоя

### Проверка здоровья бота (первые 24 часа)

```bash
# Статус PM2
pm2 status

# Логи в реальном времени
pm2 logs forex-bot --lines 50

# Проверка критических сообщений
pm2 logs forex-bot --lines 500 | grep -i "error\|critical\|failed"

# Проверка data quality работает
pm2 logs forex-bot --lines 500 | grep "filterForDelivery\|skipped"
```

### Ожидаемые значения в логах

```
[CalendarService] Successfully parsed 5 events from ForexFactory
[MyfxbookService] Found 87 events from Myfxbook
[Aggregation] ForexFactory: 5 events, Myfxbook: 87 events
[Aggregation] After deduplication: 79 events (8 duplicates removed)
[Bot] AI Forecast: 3 events skipped due to quality issues
[Bot] Sent 12 events for AI analysis
```

---

## 🐛 Troubleshooting

### Проблема: TypeScript ошибки при сборке

```bash
# Переустановить зависимости
rm -rf node_modules package-lock.json
npm install
npm run build
```

### Проблема: База данных не обновляется

DataQualityService автоматически создаст таблицу `data_issues` при первом использовании.

Если проблема:
```bash
# Проверить структуру БД
sqlite3 forex-news.db ".schema"

# Если нужно - пересоздать
sqlite3 forex-news.db < scripts/init-db.sql
```

### Проблема: AI Forecast/Results не фильтруют события

Проверьте логи:
```bash
pm2 logs forex-bot --lines 300 | grep "filterForDelivery"
```

Должно быть:
```
[DataQualityService] filterForDelivery called with mode: ai_forecast
[DataQualityService] Filtered 15 -> 12 events (3 skipped)
```

### Проблема: Нет алертов админам

Проверьте `.env`:
```bash
cat .env | grep ADMIN_CHAT_ID
```

ADMIN_CHAT_ID должен быть заполнен корректным Telegram Chat ID.

Проверьте throttling (1 алерт в час на тип):
```bash
pm2 logs forex-bot --lines 500 | grep "throttled\|alert"
```

---

## 🎉 Результат после деплоя

Бот будет:

✅ **Показывать правильное время событий** (исправлена таймзона ForexFactory)  
✅ **AI Forecast анализирует только актуальные события** (не прошедшие)  
✅ **AI Results показывает полные данные** (прогноз И факт)  
✅ **Умная дедупликация** - приоритет событиям с полными данными  
✅ **Обнаружение конфликтов** между источниками  
✅ **Автоматические отчеты о качестве** в админ-чат  
✅ **Алерты на критические проблемы** с данными  

---

## 📚 Дополнительная документация

- `DATA_QUALITY_AUDIT_REPORT.md` - полный аудит с найденными проблемами
- `DATA_QUALITY_IMPLEMENTATION_SUMMARY.md` - краткое описание реализации
- `TESTING_REPORT.md` - результаты всех тестов
- `FINAL_DEPLOYMENT_GUIDE.md` - общее руководство по деплою

---

## 📞 Если что-то пошло не так

1. Проверьте логи: `pm2 logs forex-bot --lines 500`
2. Проверьте статус: `pm2 status`
3. Перезапустите: `pm2 restart forex-bot`
4. Откатитесь к предыдущей версии:
   ```bash
   git log --oneline -5
   git checkout <previous-commit-hash>
   npm run build
   pm2 restart forex-bot
   ```

**Готово к деплою!** 🚀

---

**Дата обновления:** 29 января 2026  
**Версия:** Data Quality System v1.0
