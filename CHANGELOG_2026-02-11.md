# Changelog - February 11, 2026

## Fix: Events from Different Sources No Longer Deduplicated (Cross-Source Fix)

### Summary
Исправлена проблема дедупликации новостей из разных источников. Теперь новости из ForexFactory и Myfxbook отображаются независимо, даже если они представляют одно и то же реальное событие.

### Problem
До исправления новости из разных источников с одинаковым временем и валютой считались дубликатами. Система выбирала "лучшую" новость по приоритету, что вводило пользователей в заблуждение.

**Примеры проблемы:**
- Если новость "Non-Farm Payrolls" была и в ForexFactory, и в Myfxbook → показывалась только одна
- При выборе "Both" пользователь не видел все доступные источники
- При выборе "ForexFactory" могла показаться новость из Myfxbook (если она имела лучшие данные)

### Solution
**Изменения в `src/utils/eventAggregation.ts`:**
1. Функция `deduplicationKey` теперь включает источник в ключ: `source_time_currency`
2. Логика дедупликации работает только внутри одного источника
3. События из разных источников никогда не считаются дубликатами

```typescript
// БЫЛО:
return md5(`${timeKey}_${event.currency}`);

// СТАЛО:
return md5(`${event.source}_${timeKey}_${event.currency}`);
```

### Changes

#### Modified Files:
1. **src/utils/eventAggregation.ts**
   - Обновлена функция `deduplicationKey` - добавлен источник в ключ
   - Упрощена логика дедупликации (убран приоритет FF над Myfxbook)
   - Обновлены JSDoc комментарии

#### New Files:
1. **scripts/test-no-cross-source-dedup.ts**
   - Тестовый скрипт для проверки логики дедупликации
   - 4 тестовых сценария (все пройдены успешно ✅)

2. **CROSS_SOURCE_FIX_2026-02-11.md**
   - Подробная документация исправления
   - Описание поведения до и после
   - Влияние на пользователей

### Testing
- ✅ Тест 1: Одна новость из обоих источников → показываются ОБЕ
- ✅ Тест 2: Дубликат внутри одного источника → показывается ОДИН
- ✅ Тест 3: Смешанный сценарий → корректная дедупликация
- ✅ Тест 4: Разные новости из разных источников → показываются ВСЕ
- ✅ Линтер: ошибок не найдено

### Impact
**Положительные эффекты:**
- 📈 Пользователи с "Both" видят все события из обоих источников
- 🎯 Каждый источник отображается со своим тегом (прозрачность)
- 🔍 Возможность сравнивать данные из разных источников
- ✅ Соответствует ожиданиям: два независимых источника = два набора данных

**Что НЕ изменилось:**
- Дедупликация внутри источника работает как прежде
- Выбор источника через настройки работает как прежде
- Cross-source conflict detection продолжает работать (для мониторинга)

**Breaking Changes:** Нет (обратно совместимо)

### Related Documentation
- `CROSS_SOURCE_FIX_2026-02-11.md` - Подробное описание исправления

---

## Critical Data Quality Alerts - Switched to Manual Mode

### Summary
Автоматические алерты о критических проблемах качества данных были отключены. Теперь они отправляются только по запросу.

### Changes

#### Modified Files:
1. **src/services/CalendarService.ts**
   - Закомментирован автоматический вызов `sendCriticalDataAlert`
   - Добавлен комментарий с инструкцией использования ручных скриптов

2. **src/services/MyfxbookService.ts**
   - Закомментирован автоматический вызов `sendCriticalDataAlert`
   - Добавлен комментарий с инструкцией использования ручных скриптов

#### New Files:
1. **CRITICAL_ALERTS_MANUAL.md**
   - Полная документация по использованию алертов в ручном режиме
   - Инструкции по восстановлению автоматических алертов при необходимости
   - Рекомендации по графику проверок

2. **scripts/send-critical-alert-manual.ts**
   - Новый скрипт для ручной отправки критических алертов
   - Проверяет проблемы за последний час
   - Фильтрует только критические типы проблем

3. **CHANGELOG_2026-02-11.md**
   - Этот файл с описанием изменений

#### Updated Files:
1. **DATA_QUALITY_IMPLEMENTATION_SUMMARY.md**
   - Добавлена секция о критических алертах
   - Обновлен список будущих улучшений

### Rationale
Автоматические алерты отправлялись слишком часто и могли создавать информационный шум в админ-чате. Переход на ручной режим позволяет:
- Контролировать частоту отправки отчетов
- Получать сводную информацию за период (24 часа)
- Избежать спама при временных проблемах с парсингом
- Сохранять всю функциональность для экстренных случаев

### How to Use

#### Daily Quality Report (Recommended)
```bash
npx ts-node scripts/daily-quality-report.ts
```
Отправляет подробный отчет за последние 24 часа в админ-чат.

#### Manual Critical Alert
```bash
npx ts-node scripts/send-critical-alert-manual.ts
```
Отправляет критические проблемы за последний час (для срочных случаев).

#### View Issues in Database
```bash
npx ts-node scripts/view-data-issues.ts
```
Просмотр всех проблем в базе данных с возможностью фильтрации.

### Throttling Protection
Функция `sendCriticalDataAlert` имеет встроенную защиту от спама:
- Один и тот же тип алерта отправляется не чаще раза в час
- Это работает даже если автоматические алерты включены обратно

### Rollback Instructions
Если необходимо вернуть автоматические алерты, раскомментируйте блоки кода в:
- `src/services/CalendarService.ts` (строки ~333-343)
- `src/services/MyfxbookService.ts` (строки ~395-405)

Подробные инструкции в `CRITICAL_ALERTS_MANUAL.md`.

### Testing
- ✅ Компиляция TypeScript: успешно
- ✅ Линтер: ошибок не найдено
- ✅ Импорты сохранены для возможности быстрого восстановления
- ✅ Функция `sendCriticalDataAlert` остается доступной для ручного использования

### Related Documentation
- `CRITICAL_ALERTS_MANUAL.md` - Полное руководство по ручным алертам
- `DATA_QUALITY_IMPLEMENTATION_SUMMARY.md` - Обновлена информация о системе
- `DATA_QUALITY_MODULE.md` - Основная документация по Data Quality

### Impact
- **Breaking Changes:** Нет
- **API Changes:** Нет
- **User Impact:** Нет (пользователи не видят алерты, только админы)
- **Admin Impact:** Необходимо запускать скрипты вручную для получения отчетов

### Recommended Schedule
- **Ежедневно:** Запускать `daily-quality-report.ts` один раз в день (утром)
- **По необходимости:** Использовать `send-critical-alert-manual.ts` для срочных проверок
- **Еженедельно:** Просматривать накопленные проблемы через `view-data-issues.ts`
