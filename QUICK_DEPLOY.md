# ⚡ Быстрый деплой на сервер

## 🚀 Команды для копирования (все сразу)

```bash
# 1. Подключиться к серверу
ssh root@YOUR_SERVER_IP

# 2. Перейти в директорию бота
cd /root/forex-bot

# 3. Обновить код
git pull origin main

# 4. Установить зависимости
npm install

# 5. Пересобрать
npm run build

# 6. Перезапустить (PM2)
pm2 restart forex-bot

# 7. Проверить логи
pm2 logs forex-bot --lines 100
```

---

## ✅ Что проверить после деплоя

1. **Логи без ошибок:**
   ```bash
   pm2 logs forex-bot --lines 200 | grep -i "error"
   ```

2. **DataQuality работает:**
   ```bash
   pm2 logs forex-bot --lines 200 | grep "filterForDelivery"
   ```

3. **В Telegram:** `/daily` → кнопка **"🧠 AI Forecast"** работает

4. **Ежедневный отчет:**
   ```bash
   npm run quality:report
   ```

---

## 🔥 Если что-то сломалось

```bash
# Откат к предыдущей версии
git log --oneline -5
git checkout ae44045  # предыдущий коммит
npm run build
pm2 restart forex-bot
```

---

📖 **Полная инструкция:** `DEPLOY_DATA_QUALITY.md`
