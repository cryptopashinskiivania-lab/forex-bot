# 🔒 Руководство по безопасности

## ✅ Что уже защищено

### 1. Переменные окружения
- ✅ Все чувствительные данные вынесены в `.env`
- ✅ `.env` добавлен в `.gitignore`
- ✅ Создан `.env.example` с шаблоном без реальных значений

### 2. Git
- ✅ IP-адреса удалены из кода
- ✅ SSH команды обезличены
- ✅ URL репозитория заменен на плейсхолдер

### 3. База данных
- ✅ `bot.db` в `.gitignore`
- ✅ Все `.db` файлы и связанные журналы исключены

## 🔐 Что нужно защищать

### Критичные данные (НИКОГДА не коммитить):
1. **Токены и ключи API**
   - `BOT_TOKEN` - токен Telegram бота
   - `GROQ_API_KEY` - ключ Groq API
   - Любые другие API ключи

2. **Данные сервера**
   - IP-адреса серверов
   - SSH ключи и пароли
   - Учетные данные базы данных

3. **Персональные данные**
   - Telegram ID пользователей
   - История сообщений
   - База данных бота

## 📋 Чеклист перед коммитом

Перед каждым `git commit` проверьте:

- [ ] Нет реальных токенов в коде
- [ ] Нет IP-адресов серверов
- [ ] Нет паролей или ключей
- [ ] `.env` не добавлен в commit
- [ ] `bot.db` не добавлен в commit

### Быстрая проверка:

```bash
# Проверить, что будет закоммичено
git diff --staged

# Найти подозрительные данные
git diff --staged | grep -i "token\|password\|api_key\|secret"
```

## 🚀 Настройка нового окружения

### 1. Создайте `.env` файл:

```bash
cp .env.example .env
```

### 2. Заполните переменные:

```env
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxx
ADMIN_CHAT_ID=123456789
```

### 3. Проверьте права доступа (Linux/Mac):

```bash
chmod 600 .env
```

## 🔄 Деплой на сервер

### Безопасная передача переменных:

**❌ НЕ делайте так:**
```bash
# НЕ отправляйте .env по почте/мессенджеру
# НЕ публикуйте в Slack/Discord
# НЕ добавляйте в git
```

**✅ Делайте так:**

1. **Через SSH:**
```bash
ssh your_server
nano /path/to/project/.env
# Вставьте значения вручную
```

2. **Через защищенный менеджер паролей:**
   - 1Password
   - Bitwarden
   - LastPass

3. **Через переменные окружения CI/CD:**
   - GitHub Secrets
   - GitLab CI Variables
   - Vercel Environment Variables

## 🛡️ Что делать если токен утёк

### 1. Telegram Bot Token:

```bash
# 1. Перейдите к @BotFather в Telegram
# 2. Отправьте /mybots
# 3. Выберите бота
# 4. Нажмите "API Token"
# 5. Нажмите "Revoke current token"
# 6. Скопируйте новый токен в .env
```

### 2. GROQ API Key:

1. Войдите на https://console.groq.com
2. Перейдите в API Keys
3. Удалите скомпрометированный ключ
4. Создайте новый ключ
5. Обновите `.env`

### 3. Очистка Git истории (если случайно закоммитили):

```bash
# ВНИМАНИЕ: Это переписывает историю!
# Используйте только если точно понимаете что делаете

# 1. Удалить файл из всей истории
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch .env" \
  --prune-empty --tag-name-filter cat -- --all

# 2. Форсированный push (если уже был push)
git push origin --force --all

# 3. Сменить токены/ключи (они уже могли быть скомпрометированы!)
```

## 🔍 Регулярные проверки

### Еженедельно:

```bash
# Проверить, что .env не в git
git ls-files | grep .env

# Должен быть пустой вывод (или только .env.example)
```

### Перед важным деплоем:

```bash
# Найти все упоминания токенов в коде
rg -i "token|password|secret|api_key" --type-not md

# Проверить .gitignore
cat .gitignore | grep -E "\.env|\.db"
```

## 📚 Дополнительные ресурсы

- [OWASP API Security Top 10](https://owasp.org/www-project-api-security/)
- [Git-secrets tool](https://github.com/awslabs/git-secrets)
- [Gitleaks scanner](https://github.com/zricethezav/gitleaks)

## ⚠️ Напоминание

**Безопасность - это не одноразовая задача, а постоянный процесс.**

- Регулярно ротируйте ключи
- Проверяйте логи на подозрительную активность
- Обновляйте зависимости (`npm audit`)
- Никогда не доверяйте данным от пользователей

---

**Вопросы по безопасности?** Создайте Issue в репозитории.
