# Сводка по переходу на Multi-User архитектуру

## ✅ Задача выполнена

Бот успешно переработан для поддержки **неограниченного количества пользователей** с **персональными настройками**.

## 🎯 Решенная проблема

**Было:** Настройки хранились глобально. Когда один пользователь менял настройки (включал/выключал валюты), это влияло на ВСЕХ пользователей.

**Стало:** Каждый пользователь имеет свои персональные настройки. Изменения одного пользователя не влияют на других.

## 📊 Что изменено

### 1. База данных (database.ts)

#### Новая таблица `users`
Хранит информацию о всех зарегистрированных пользователях:
- user_id (PRIMARY KEY)
- username, first_name, last_name
- registered_at (timestamp)

#### Обновленная таблица `user_settings`
Теперь с составным первичным ключом `(user_id, key)`:
- Каждая настройка привязана к конкретному пользователю
- Foreign key на таблицу users (CASCADE DELETE)

#### API изменения
Все методы теперь принимают `userId`:
```typescript
// Было:
database.getMonitoredAssets()
database.toggleAsset(asset)

// Стало:
database.getMonitoredAssets(userId)
database.toggleAsset(userId, asset)
```

Новые методы:
- `registerUser(userId, username?, firstName?, lastName?)` - регистрация пользователя
- `getUsers()` - получить всех пользователей
- `getUserById(userId)` - получить пользователя по ID

### 2. Bot.ts

#### Автоматическая регистрация
Добавлен middleware для автоматической регистрации:
```typescript
bot.use(async (ctx, next) => {
  if (ctx.from) {
    database.registerUser(
      ctx.from.id,
      ctx.from.username,
      ctx.from.first_name,
      ctx.from.last_name
    );
  }
  await next();
});
```

#### Команды и callbacks
- `/settings` - работает с настройками конкретного пользователя
- Все toggle handlers передают `ctx.from.id` в database методы
- `buildSettingsKeyboard(userId)` - строит клавиатуру для конкретного пользователя

### 3. SchedulerService

#### Рассылка всем пользователям
**Было:** Отправка только одному админу (ADMIN_CHAT_ID)
```typescript
await bot.api.sendMessage(adminChatId, message);
```

**Стало:** Рассылка всем зарегистрированным пользователям
```typescript
const users = database.getUsers();

for (const user of users) {
  const monitoredAssets = database.getMonitoredAssets(user.user_id);
  const userEvents = events.filter(e => monitoredAssets.includes(e.currency));
  
  if (userEvents.length > 0) {
    await bot.api.sendMessage(user.user_id, formatMessage(userEvents));
  }
}
```

#### Персональные настройки
- `isQuietHours(userId)` - проверяет тихие часы для конкретного пользователя
- `shouldSendReminder(event, userId)` - учитывает настройки пользователя
- RSS отправляется только если включен для пользователя

#### Tracking отправленных новостей
Теперь per-user:
```typescript
// Было: reminder_eventId
// Стало: reminder_userId_eventId
const reminderId = `reminder_${userId}_${id}`;
```

### 4. Services (CalendarService, MyfxbookService, RssService)

#### Удалена фильтрация по активам
Сервисы больше НЕ фильтруют события по `monitoredAssets`:
- Возвращают ВСЕ события (High и Medium impact)
- Фильтрация по валютам происходит в SchedulerService для каждого пользователя

**Было:**
```typescript
const monitoredAssets = database.getMonitoredAssets();
const allowed = ALLOWED_CURRENCIES.has(currency);
```

**Стало:**
```typescript
// Фильтруем только по impact, не по валютам
const allowed = (impact === 'High' || impact === 'Medium');
```

### 5. Scripts

Обновлены все скрипты для работы с multi-user:

- `migrate-multi-user.ts` - миграция на новую структуру
- `check-db-assets.ts` - проверка настроек всех пользователей
- `init-monitored-assets.ts` - инициализация для конкретного пользователя
- `migrate-db-assets.ts` - массовое обновление настроек всех пользователей

## 🚀 Как использовать

### Для пользователей

1. **Начать работу:**
   ```
   /start
   ```
   Бот автоматически зарегистрирует вас

2. **Настроить активы:**
   ```
   /settings
   ```
   - Выберите валюты (USD, EUR, GBP, JPY, CAD, AUD, NZD, CHF, XAU, BTC, OIL)
   - Включите/выключите RSS (внешние источники)
   - Настройте тихие часы (23:00-08:00 Kyiv)

3. **Получать уведомления:**
   - Бот отправляет уведомления только о ваших активах
   - Учитываются ваши настройки тихих часов
   - Напоминания за 15 минут до события
   - Результаты событий с анализом

### Для администраторов

#### Первоначальная настройка

1. **Миграция базы данных:**
   ```bash
   npx ts-node scripts/migrate-multi-user.ts
   ```
   
2. **Проверка настроек:**
   ```bash
   npx ts-node scripts/check-db-assets.ts
   ```

3. **Запуск бота:**
   ```bash
   npm run dev
   ```

#### Управление пользователями

```typescript
// Получить всех пользователей
const users = database.getUsers();

// Получить настройки пользователя
const assets = database.getMonitoredAssets(userId);
const rssEnabled = database.isRssEnabled(userId);

// Изменить настройки пользователя
database.setAssets(userId, ['USD', 'EUR']);
database.toggleRss(userId);
```

## 📈 Преимущества

✅ **Масштабируемость** - Неограниченное количество пользователей

✅ **Изоляция данных** - Настройки каждого пользователя независимы

✅ **Персонализация** - Каждый пользователь получает только интересующие его новости

✅ **Гибкость** - Различные настройки для разных пользователей

✅ **Автоматизация** - Автоматическая регистрация при первом взаимодействии

✅ **Удобство** - Простая настройка через команду `/settings`

## 🔧 Технические детали

### Структура базы данных

```sql
-- Таблица пользователей
CREATE TABLE users (
  user_id INTEGER PRIMARY KEY,
  username TEXT,
  first_name TEXT,
  last_name TEXT,
  registered_at INTEGER NOT NULL
);

-- Таблица настроек (per-user)
CREATE TABLE user_settings (
  user_id INTEGER NOT NULL,
  key TEXT NOT NULL,
  value TEXT,
  PRIMARY KEY (user_id, key),
  FOREIGN KEY (user_id) REFERENCES users(user_id) ON DELETE CASCADE
);

-- Таблица отправленных новостей (global)
CREATE TABLE sent_news (
  id TEXT PRIMARY KEY,
  created_at INTEGER
);
```

### Настройки пользователя (user_settings)

Ключи:
- `monitored_assets` - JSON array валют, например: `["USD","EUR","GBP"]`
- `RSS_ENABLED` - boolean string: `"true"` или `"false"`
- `QUIET_HOURS_ENABLED` - boolean string: `"true"` или `"false"`

### Per-User Notification IDs

Формат ID для tracking:
- Reminders: `reminder_${userId}_${eventId}`
- Results: `result_${userId}_${eventId}`
- Events: `event_${userId}_${eventId}`
- RSS: `rss_${userId}_${itemId}`

## 📝 Миграция существующих данных

При первом запуске:
1. База данных автоматически пересоздается с новой структурой
2. Если установлен `ADMIN_CHAT_ID` в .env, запустите скрипт миграции
3. Все новые пользователи регистрируются автоматически при `/start`

## 🧪 Тестирование

### Проверка изоляции настроек

1. Создайте 2 тестовых Telegram аккаунта
2. Отправьте `/start` с обоих
3. Настройте разные активы:
   - Пользователь A: USD, EUR
   - Пользователь B: GBP, JPY
4. Проверьте, что каждый получает только свои новости

### Проверка в базе данных

```bash
npx ts-node scripts/check-db-assets.ts
```

Вывод покажет всех пользователей и их настройки.

## 📚 Документация

Подробная документация доступна в:
- `MULTI_USER_MIGRATION.md` - Полное руководство по миграции
- `IMPLEMENTATION_SUMMARY.md` - Общая архитектура бота

## ⚠️ Breaking Changes

Это **breaking change**. После обновления:

❌ Старая база данных несовместима (нужна миграция)

❌ ADMIN_CHAT_ID теперь опциональный (используется только для миграции)

❌ Все методы database требуют userId

✅ Но миграция автоматическая при первом запуске!

## 🎉 Результат

Бот теперь полностью готов к работе с **большим количеством пользователей**!

Каждый пользователь:
- Регистрируется автоматически
- Настраивает свои активы через `/settings`
- Получает персональные уведомления
- Имеет изолированные настройки

**Проблема решена: настройки больше не пересекаются между пользователями! ✅**
